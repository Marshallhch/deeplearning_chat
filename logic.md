1. intent 모델 학습

- 예: 짜장면 배달해 주세요 -> 주문

2. ner 모델 학습

- 예: 짜장면 배달해 주세요 -> 짜장면(음식) B_FOOD, 배달(행동) B_ACT

3. 음식 주문 챗봇 생성

- 주문 intent 예측 후 데이터베이스에서 주문에 해당되는 값을 출력

4. 전체 로직 플로우

# 전처리 객체 생성 -> 의도 분류 모델 객체 생성 -> 개체명 인식 -> 클라이언트의 서버 연결 -> 데이터베이스 연결 -> 클라이언트 데이터 수신 -> 의도 파악 및 개체명 파악 -> 답변 검색 -> 답변을 json 형식으로 반환 -> 서버 실행(포트, 최대 연결 수 지정) -> 스레드 시작
